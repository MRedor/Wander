package main

func routesBuild(xLat float64, xLon float64, yLat float64, yLon float64) string {

	// прочитать все объекты из БД в квадрате, описанном координатами

	// в зависимости от типа маршрута - выбрать точки в маршрут

	// через OSRM построить путь между набором точек

	// сохранить маршрут в БД, вернуть всё получившиеся из метода

	return "some structure"
}
